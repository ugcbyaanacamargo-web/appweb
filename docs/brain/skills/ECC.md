# Skill card: ECC

upstream: `vendor/engines/ecc`

ECC é biblioteca especializada, não o controlador do ciclo.

Carregue somente a especialidade necessária, principalmente:
- pesquisa/documentação;
- `api-design`;
- `backend-patterns`;
- `frontend-patterns`;
- segurança;
- E2E;
- verificação.

Quando a superfície `.agents/skills/` existir no upstream fixado, prefira-a como instrução portátil; use `skills/` quando for a fonte correspondente. Não carregue o catálogo inteiro.
